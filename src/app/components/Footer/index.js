import React from 'react';

export default ({  }) => {
  return (
    <footer style={styles.wrapper} >
      <aside>
        &copy; Footer.
      </aside>
    </footer>
  );
};

const styles = {
  wrapper: {
    borderTop: '1px solid black',
    margin: '10px 0 0 0',
  },
};
